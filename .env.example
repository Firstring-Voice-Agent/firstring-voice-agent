# === FirstRing AI — Voice Agent ===
# Copy this file to ".env" for local development.
# On Render, set the real values in Dashboard → Environment.

# --- Hosting ---
PUBLIC_BASE_URL=https://firstring-voice-agent.onrender.com   # no trailing slash

# --- AI Keys ---
OPENAI_API_KEY=sk-...            # OpenAI for replies + fallback TTS
DEEPGRAM_API_KEY=dg_...          # Deepgram for live speech-to-text
ELEVENLABS_API_KEY=...           # (optional) ElevenLabs for TTS
ELEVENLABS_VOICE_ID=...          # (optional) ElevenLabs voice ID

# --- Twilio (optional) ---
TWILIO_AUTH_TOKEN=...            # For request validation (not required for MVP)

# --- Business Config ---
BUSINESS_NAME=FirstRing AI       # Used in system prompt for receptionist

# --- Logging ---
LOG_LEVEL=debug                  # options: error, warn, info, debug
